version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: JD
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: job_crawler
    ports:
      - "3306:3306"
      - "33060:33060"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
